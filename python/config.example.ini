[WEBEX]
access_token = YOUR_WEBEX_BOT_ACCESS_TOKEN
allowed_room_id = YOUR_ALLOWED_ROOM_ID

[TEAMS]
; Microsoft Teams Bot Framework settings
; The App ID and Password are usually set as environment variables for security, but you can document them here for reference.
; MICROSOFT_APP_ID and MICROSOFT_APP_PASSWORD should be set in your environment, not here.
allowed_channel_id = YOUR_ALLOWED_TEAMS_CHANNEL_ID

[TWS_API]
base_url = https://your-tws-server:31116/twsd
user = youruser
password = yourpassword
verify_ssl = false
timezone_offset =